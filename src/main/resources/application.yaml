spring:
  application.name: spring-security-demo
  profiles:
    active: local
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://keycloak:8080/realms/proselyte/protocol/openid-connect/certs

jwt:

keycloak:
  realm: proselyte
  server-url: http://keycloak:8080
  user-sync-client:
    client-id: admin-client
    client-secret: 9Mq6a6BHzUQBcuNQaPHgwMa8XkO4GZmB

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true